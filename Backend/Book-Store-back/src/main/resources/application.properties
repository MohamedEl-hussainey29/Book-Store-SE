spring.application.name=BookStore
spring.docker.compose.enabled=false

spring.datasource.url=jdbc:mysql://localhost:3306/bookstore?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=18102005#Mm
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=mohamedmostafa18102005@gmail.com
spring.mail.password=edljdjkqqtfwmori

spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.ssl.trust=smtp.gmail.com

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# Flyway / Liquibase
spring.flyway.enabled=false
spring.liquibase.enabled=false

spring.modulith.events.jdbc.enabled=false

spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB

# ✅ REMOVED this line — it was disabling Spring Security entirely:
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration

# ============================================================
# ✅ JWT Configuration (required for the new auth system)
# ============================================================
jwt.secret=your-very-secret-key-that-is-at-least-256-bits-long!
jwt.expiration=86400000